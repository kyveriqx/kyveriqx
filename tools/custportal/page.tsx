import { ToolPlaceholder } from "../../core/ui/tool-placeholder";

export default function CustPortal() {
  return (
    <ToolPlaceholder
      name="Customer Portal"
      slug="custportal"
      description="A self-service portal for your customers — orders, invoices, ledgers, statements."
    />
  );
}
