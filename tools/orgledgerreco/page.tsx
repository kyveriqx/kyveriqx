import { ToolPlaceholder } from "../../core/ui/tool-placeholder";

export default function OrgLedgerReco() {
  return (
    <ToolPlaceholder
      name="Org Ledger Reconciliation"
      slug="orgledgerreco"
      description="Reconcile ledgers across entities within your org and find the gaps in minutes."
    />
  );
}
