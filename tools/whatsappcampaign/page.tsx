import { ToolPlaceholder } from "../../core/ui/tool-placeholder";

export default function WhatsappCampaign() {
  return (
    <ToolPlaceholder
      name="WhatsApp Campaigns"
      slug="whatsappcampaign"
      description="Send templated WhatsApp campaigns to your customer list. Trigger.dev job handles batches and retries."
    />
  );
}
